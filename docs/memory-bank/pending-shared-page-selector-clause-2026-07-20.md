# 草案: CLAUDE.md §6「非自明な落とし穴」への1行追加（PM 承認待ち）

> `/flywheel` 2026-07-20（工程10）起票。CLAUDE.md は憲法＝**PM 承認必須**・直書き禁止（protect-paths）。
> PM が承認したら §6 に統合し、この pending を削除する。却下なら理由を1行残して削除。
> 詳細な決定は [[pending-adr-0020-shared-page-selector-scope-2026-07-20]]（ADR-0020 草案）。

## 観察

下流が共有ページ（`/reports` 等）に list/labeled 要素を1つ足すと、そのページを `getByRole('list')` /
`getByLabel(...)` の単一前提で叩く受け入れテスト（先行スライスのものを含む）が strict-mode 二重マッチで赤。
per-slice では検出不能、工程9b でのみ露見。**2回発生**（slice-10→27・slice-15→28。incidents.md #1/#2）。

## 分類

**ハーネスの欠陥**（工程4 翻訳ガイドに共有ページのセレクタ規約が無い）。スライス設計の欠陥ではない
（追加要素は仕様どおり・スライスは大きすぎない）。

## 発生回数

**2回目**（2ストライク成立）→ 宣言化。宣言済みで3回目が出たら acceptance lint（事後検証）へ昇格。

## §6 追加候補（1行）

```
- **共有ページ（`/reports` 等）を叩く受け入れセレクタは親スコープで限定する。** 下流が list/labeled 要素を
  1つ足すと、`getByRole('list')`/`getByLabel(...)` のページ全体単一前提が strict-mode で二重マッチし、
  工程9b でのみ赤くなる（per-slice では出ない）。`within(getByTestId(...))` 等でそのスライスの1要素に絞る（ADR-0020）。
```

## 剪定提案（1行足すなら1行削れないか）

- §6 の現行行はいずれも「1行消すと Claude がミスする」水準で、明白に削れる行は無い。
  ただし本追加を入れるなら §6 が7項目→8項目になるため、**将来 §6 が肥大したら
  「422 を返す」「非同期エラーは next() に渡らない」等の Express 個別注意は
  `express-review-rules` skill 側へ移して §6 から落とす**候補（今回は据え置き・別 flywheel 案件）。
