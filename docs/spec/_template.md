---
slice: slice-NN-<slug>
approved: false          # PM がここを true にするまで Phase B は受け入れテストに1文字も書けない
---

<!--
  仕様表テンプレート（`/spec <slice>` Phase A が埋める。正本は PM）

  ルール:
  - 受入基準は **3〜5個**。6個以上ならスライスを分割する。
  - 各基準に必ず `source:` を書く。
      source: 画面仕様 … answer key（docs/画面仕様・必要画面・機能一覧）の書き起こし
      source: PM      … 画面仕様に無い＝**本当の設計**。ここだけ PM が重点レビュー
  - 「未定」と書いてよい。**AI に埋めさせない。** 未定が残るなら approved: true にしない。
  - 用語は CONTEXT.md に従う。ズレたら用語を先に直す。
  - ステータスコードの根拠を区別する: 仕様（画面仕様）か、実装規約（ZodError→400・ErrorKind→KIND_TO_STATUS）か。
  - **API の AC だけで 3〜5個 を埋めない**（ADR-0018）。画面があるスライスは
    「画面要件」に UI の AC を必ず立てる。**画面仕様に無い UI 挙動は `source: PM`**
    ＝本当の設計。**工程4 の翻訳者が埋める空白ではない。ここで決め切る。**
  - 画面設計そのもの（docs/画面仕様 の該当ページ）が無いなら、ここで埋めずに**工程1 へ戻す**。
    凍結ゲート（ADR-0016）の対象は画面仕様であって、この仕様表ではない。別の成果物なので混同しない。
-->

## AC-1 <一文で書く受入基準>

- source: 画面仕様
- **Given** <前提>
- **When** <操作>
- **Then** <観測できる結果。ステータスコード・画面表示など>

## AC-2 <...>

- source: PM        # ★画面仕様に無い新規決定。理由を1行:
- 理由: <なぜ画面仕様に無い挙動を決めるのか>
- **Given** ...
- **When** ...
- **Then** ...

---

## 合成フィクスチャ（PM 所有）

**本番/実データは一切入れない**（憲法 §1-7）。迷ったら厳しい側（上位）に倒す。

| フィールド | 階層 | 例 | 備考 |
|---|---|---|---|
| body | L0 | "本日の報告" | 非個人データ |
| engineer_name | L1 | "山田 太郎" | 本人 PII（合成） |
| care_note | L2 | ―（このスライスでは扱わない） | 要配慮個人情報・第三者 PII |

---

## 画面要件（必須。「あれば」ではない・ADR-0018）

**このセクションを空欄のまま `approved: true` にしない。** 画面を持たないスライスなら
「画面なし」＋理由を書く。それが唯一の免除経路であり、**押すのは PM／AIアーキ**（黙って省略できない）。

- 対象画面: <docs/画面仕様/ の該当ページ。無いなら「画面なし」＋理由>
- **UI の受入基準**: 上の AC-n として立てたか（画面があるのに UI の AC が0個なら**未完成**）
  - 画面仕様の design-example 画像も根拠にする。仕様に無い UI 挙動は **`source: PM`**。理由行を必ず書く。
- **UI 検証の形**（どちらかを明記する）:
  - **RTL（現行）** … `apps/web/src/__test__/<feature>/<name>.acceptance.test.tsx`。
    `role` / `label` ベースの DOM アサーションで検証する。
    検証する要素: <getByRole('button', {name: '...'}) など、具体的に列挙する>
  - **E2E / golden（Playwright 導入後）** … `e2e/` ＋ golden 撮影。マスクする要素も書く。
  - **「検証しづらい」は「UI 検証をしない」理由にならない**（ADR-0018）。

> 判定の対象は `*.acceptance.test.tsx`（実装コンポーネントを import して render 必須）。
> **画面があるのに UI テストが0本なら工程4 は完了しない**（ADR-0018）。

---

## スコープ外（明示する）

- <このスライスでは扱わないこと。別スライスへ>
